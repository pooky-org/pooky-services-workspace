name: Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Setup
        uses: ./.github/actions/setup-yarn

      - name: Lint
        run: yarn lint
